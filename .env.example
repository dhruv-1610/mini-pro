# ──────────────────────────────────────────
# CleanupCrew — Environment Variables
# Copy this file to .env and fill in values.
# ──────────────────────────────────────────

# General
NODE_ENV=development
PORT=4000
API_HOST=0.0.0.0

# MongoDB
MONGO_URI=mongodb://mongo:27017/cleanupcrew

# JWT — Access Token
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=15m

# JWT — Refresh Token
JWT_REFRESH_SECRET=change-me-refresh-secret
JWT_REFRESH_EXPIRES_IN=7d

# Stripe
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# CORS — comma-separated list of allowed origins (strict whitelist)
ALLOWED_ORIGINS=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# File Uploads
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880

# Logging
LOG_LEVEL=info

# ──────────────────────────────────────────
# Email (SMTP) — optional; if not set, verification emails are skipped (log only)
# Set these to send verification emails after registration.
# ──────────────────────────────────────────
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=CleanupCrew <noreply@yourdomain.com>
# FRONTEND_URL=http://localhost:5173

# ──────────────────────────────────────────
# Frontend (services/web) — optional for dev; required for production build
# ──────────────────────────────────────────
# VITE_API_URL=http://localhost:4000
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
