# Frontend env (Vite). Copy to .env and set values.
# Required for Donate page (Stripe):
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here

# Optional: API base URL (default in dev is proxy to same host)
# VITE_API_URL=http://localhost:4000
